{
  "name": "toerilla-backend",
  "version": "2.0.0",
  "description": "Toe-Rilla Gig Network — Phase 2 backend (Express + Postgres + Twilio + Stripe)",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "stripe": "^16.8.0",
    "twilio": "^5.3.0"
  }
}
